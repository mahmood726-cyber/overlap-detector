# Runtime + test dependencies.
# Pin minimum versions; the CI workflow (.github/workflows/ci.yml) installs
# these for both lint and test jobs.

matplotlib>=3.10.9
numpy>=1.24
pyreadr>=0.5
pytest>=8.0
